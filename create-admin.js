const { PrismaClient } = require("@prisma/client"); const bcrypt = require("bcryptjs"); const prisma = new PrismaClient(); async function main() { console.log("Creando usuario administrador..."); try { const hashedPassword = await bcrypt.hash("admin123", 10); const adminUser = await prisma.user.upsert({ where: { email: "admin@peluqueria.com" }, update: {}, create: { name: "Administrador", email: "admin@peluqueria.com", password: hashedPassword, role: "ADMINISTRADOR" } }); console.log("Usuario creado:", adminUser.email); console.log("Contraseña: admin123"); } catch (error) { console.error("Error:", error.message); } finally { await prisma.$disconnect(); } } main();
